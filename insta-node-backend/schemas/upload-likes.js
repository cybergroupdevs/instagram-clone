const user = require('./user-details');
const mongoose = require('mongoose');
const schema = 
module.exports = {
    OwnerId: {
        type: user-detail
    },
    UploadId: {
        type: user-detail
    }
}